# FlowRep models

model files are in OBJ format

network files are in OBJ format, but they only contain lists of vertices and edges, not faces.

pre and post regularization networks are provided for bathtub, mouse, mug, phone, trebol.
